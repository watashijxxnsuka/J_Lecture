package chap20_enum.enums;

public enum RGB {
	
	// RED,GREEN,BLUE 상수선언
	// 값은 4개를 갖는다
	// 첫번째 값은 소문자 색상이름 
	// 2,3,4 번째 값은 int 
	// 두번째 값은 빨간색 농도(255가 최대값)
	// 세번째 값은 초록색 농도(255가 최대값)
	// 네번째 값은 파란색 농도(255가 최대값)
	// enum을 구현하세요.
	
	R(red, 255, 0, 0),
	G(green, 255, 0, 0),
	B(blue, 255, 0, 0);

}
